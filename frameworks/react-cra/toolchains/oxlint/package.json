{
  "scripts": {
    "lint": "oxlint",
    "format": "prettier",
    "check": "prettier --write . && oxlint --fix"
  },
  "devDependencies": {
    "oxlint": "^1.18.0",
    "prettier": "^3.5.3",
    "@prettier/plugin-oxc": "^0.0.4"
  }
}
