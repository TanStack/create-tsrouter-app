{
  "scripts": {
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "check": "oxfmt && oxlint --fix"
  },
  "devDependencies": {
    "@tanstack/oxlint-config": "^0.1.0",
    "oxlint": "^1.47.0",
    "oxfmt": "^0.32.0"
  }
}
